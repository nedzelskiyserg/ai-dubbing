# Отладка проблемы "API сервер не отвечает"

## 🔍 Диагностика

Если вы видите ошибку "API сервер не отвечает", выполните следующие шаги:

### 1. Проверьте консоль Electron

**Windows:**
- Откройте DevTools: `Ctrl+Shift+I` или `F12`
- Перейдите на вкладку "Console"
- Ищите сообщения с префиксом `🔧`, `✅`, `❌`

**macOS/Linux:**
- Запустите приложение из терминала:
  ```bash
  cd frontend
  npm run electron-dev
  ```
- Смотрите логи в терминале

### 2. Проверьте, что API сервер запущен

**В консоли Electron должны быть сообщения:**
```
🔧 Запуск API сервера:
   Python: [путь]
   API Path: [путь]
   CWD: [путь]
✅ Процесс API сервера создан (PID: [номер])
⏳ Ожидание готовности API сервера...
✅ API сервер успешно запущен и готов к работе
```

### 3. Проверьте возможные проблемы

#### Проблема: "Файл не найден"

**Решение:**
- Убедитесь, что приложение собрано корректно
- Проверьте, что файл `api-server.exe` существует в:
  ```
  resources/python-backend/api-server/api-server.exe
  ```

#### Проблема: "Python не найден" (в режиме разработки)

**Решение:**
- Установите Python 3.10+
- Добавьте Python в PATH
- Или создайте виртуальное окружение:
  ```bash
  python3 -m venv .venv
  source .venv/bin/activate  # macOS/Linux
  .venv\Scripts\activate     # Windows
  ```

#### Проблема: "Порт 5001 занят"

**Решение:**
```bash
# Windows
netstat -ano | findstr :5001
taskkill /F /PID [номер_процесса]

# macOS/Linux
lsof -ti:5001 | xargs kill -9
```

#### Проблема: "API сервер завершился с ошибкой"

**Решение:**
- Проверьте логи в консоли Electron
- Ищите сообщения с "Traceback" или "Exception"
- Убедитесь, что все зависимости установлены:
  ```bash
  pip install -r requirements.txt
  ```

### 4. Ручная проверка API сервера

**Откройте терминал и проверьте:**
```bash
# Проверьте, что порт слушается
curl http://localhost:5001/api/health

# Должен вернуть: {"status": "ok"}
```

### 5. Запуск API сервера вручную (для отладки)

**Терминал 1 - API сервер:**
```bash
# Если используете venv
source .venv/bin/activate  # macOS/Linux
.venv\Scripts\activate    # Windows

# Запустите API сервер
python3 src/api_server.py
```

**Терминал 2 - Electron:**
```bash
cd frontend
npm run electron-dev
```

### 6. Проверка путей в упакованном приложении

**Windows:**
- Откройте DevTools в Electron (`Ctrl+Shift+I`)
- В консоли выполните:
  ```javascript
  require('electron').remote.process.resourcesPath
  ```
- Проверьте, что путь правильный и содержит:
  - `python-backend/api-server/api-server.exe`
  - `src/api_server.py` (fallback)

## 📝 Логи для отправки разработчику

Если проблема не решена, соберите следующую информацию:

1. **Версия ОС:** Windows 10/11, macOS версия, Linux дистрибутив
2. **Режим запуска:** Разработка или Production (упакованное приложение)
3. **Логи из консоли Electron:**
   - Все сообщения с `🔧`, `✅`, `❌`
   - Сообщения об ошибках
   - Traceback (если есть)
4. **Результат проверки:**
   ```bash
   curl http://localhost:5001/api/health
   ```
5. **Информация о процессах:**
   ```bash
   # Windows
   tasklist | findstr python
   
   # macOS/Linux
   ps aux | grep python
   ```

## 🔧 Быстрое решение

**Если ничего не помогает:**

1. Закройте приложение полностью
2. Убейте все процессы Python:
   ```bash
   # Windows
   taskkill /F /IM python.exe
   taskkill /F /IM api-server.exe
   
   # macOS/Linux
   pkill -f python
   pkill -f api-server
   ```
3. Перезапустите приложение
4. Подождите 10-30 секунд после запуска

## 💡 Полезные команды

```bash
# Проверить, что порт свободен
netstat -ano | findstr :5001  # Windows
lsof -i :5001                 # macOS/Linux

# Проверить Python
python --version
python3 --version

# Проверить зависимости
pip list | grep flask
pip list | grep flask-cors
```
