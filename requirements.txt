flask
flask-cors
pytubefix
# WhisperX v3.4.4 — последняя версия, совместимая с torch 2.6 (cu124).
# v3.5.0+ требует torch>=2.7.1, v3.7.4+ требует torch~=2.8.0.
# При переходе на cu126 можно обновить до latest.
https://github.com/m-bain/whisperx/archive/refs/tags/v3.4.4.zip
certifi
huggingface_hub>=0.20.0
pyannote.audio>=3.1.0
# torch и torchaudio устанавливаются отдельно через dependency-manager (CPU или CUDA).
# Не фиксируем версию — WhisperX сам тянет нужную.
# dependency-manager переустановит с CUDA из cu126 индекса поверх.
python-dotenv
requests
deep-translator
googletrans==4.0.0rc1
# NumPy: WhisperX 3.7.6 требует numpy>=2.1.0
# После установки numpy 2.x нужно переустановить scipy и sklearn:
# pip install --force-reinstall scipy scikit-learn
numpy>=2.1.0
librosa==0.10.2
onnxruntime>=1.19.0; sys_platform == 'darwin' or sys_platform == 'win32'
onnxruntime-gpu>=1.19.0; sys_platform == 'linux'
openai-whisper
soundfile==0.12.1
transformers>=4.40.0
pydub
scipy>=1.10.0
# Готовые голоса (Microsoft Edge Neural TTS)
edge-tts>=7.0.0
# Noise reduction for TTS output
noisereduce>=3.0.0
# Video Assembly
moviepy>=1.0.3
