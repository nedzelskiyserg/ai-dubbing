flask
flask-cors
pytubefix
# Используем версию из GitHub (zip), так как на PyPI устаревшая
# zip-формат не требует установленного git на машине пользователя
https://github.com/m-bain/whisperx/archive/refs/heads/main.zip
certifi
huggingface_hub>=0.20.0
pyannote.audio>=3.1.0
# torch и torchaudio устанавливаются отдельно (CPU-версия) через dependency-manager
# Не добавляйте их сюда — иначе pip скачает CUDA-версию (~700 МБ вместо ~150 МБ)
python-dotenv
requests
deep-translator
googletrans==4.0.0rc1
# NumPy: WhisperX 3.7.6 требует numpy>=2.1.0
# После установки numpy 2.x нужно переустановить scipy и sklearn:
# pip install --force-reinstall scipy scikit-learn
numpy>=2.1.0
# Voice Cloning (Coqui XTTS v2)
# Примечание: TTS требует Python 3.10+
# Если Python < 3.10, используйте: brew install python@3.11
# Затем создайте venv: python3.11 -m venv venv && source venv/bin/activate
# Для PyTorch 2.9+ требуется: coqui-tts[codec]
coqui-tts[codec]>=0.27.0
pydub
# Noise reduction for TTS output (убирает "квакание" и артефакты)
noisereduce>=3.0.0
scipy>=1.10.0
# Video Assembly
moviepy>=1.0.3