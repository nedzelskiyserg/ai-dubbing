flask
flask-cors
pytubefix
# Используем версию из GitHub (zip), так как на PyPI устаревшая
# zip-формат не требует установленного git на машине пользователя
https://github.com/m-bain/whisperx/archive/refs/heads/main.zip
certifi
huggingface_hub>=0.20.0
pyannote.audio>=3.1.0
# torch и torchaudio устанавливаются отдельно через dependency-manager (CPU или CUDA).
# Фиксируем версию чтобы pip не тянул несовместимую из PyPI (2.8 ломает cu124).
torch>=2.5.0,<2.7.0
torchaudio>=2.5.0,<2.7.0
python-dotenv
requests
deep-translator
googletrans==4.0.0rc1
# NumPy: WhisperX 3.7.6 требует numpy>=2.1.0
# После установки numpy 2.x нужно переустановить scipy и sklearn:
# pip install --force-reinstall scipy scikit-learn
numpy>=2.1.0
librosa==0.10.2
onnxruntime>=1.19.0; sys_platform == 'darwin' or sys_platform == 'win32'
onnxruntime-gpu>=1.19.0; sys_platform == 'linux'
openai-whisper
soundfile==0.12.1
transformers>=4.40.0
pydub
scipy>=1.10.0
# Готовые голоса (Microsoft Edge Neural TTS)
edge-tts>=7.0.0
# Noise reduction for TTS output
noisereduce>=3.0.0
# Video Assembly
moviepy>=1.0.3
