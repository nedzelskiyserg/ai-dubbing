# Как запустить AI Dubbing Studio на Windows

## 🚀 Быстрый старт (рекомендуется)

### Вариант 1: Portable версия (один EXE файл) ⭐

**Это самый простой способ - не требует установки!**

1. **Скачайте артефакт** из GitHub Actions:
   - Перейдите в раздел "Actions" вашего репозитория
   - Найдите последний успешный билд
   - Скачайте артефакт **"AI-Dubbing-Studio-Windows-Portable"**
   - Распакуйте архив

2. **Найдите файл:**
   ```
   AI-Dubbing-Studio-1.0.0-x64-Portable.exe
   ```
   
   **Или просто любой `.exe` файл в архиве** (он будет единственным)

3. **Запустите файл:**
   - Просто дважды кликните на `.exe` файл
   - Приложение запустится сразу, без установки
   - При первом запуске может занять 10-30 секунд (запуск Python backend)

4. **Готово!** Приложение откроется в окне.

**Преимущества:**
- ✅ Не требует установки
- ✅ Можно запускать с USB-флешки
- ✅ Не оставляет следов в реестре
- ✅ Можно иметь несколько версий одновременно

---

### Вариант 2: Установщик (NSIS)

**Если вы хотите установить приложение в систему:**

1. **Скачайте артефакт:**
   - Артефакт **"AI-Dubbing-Studio-Windows-Installer"**
   - Распакуйте архив

2. **Запустите установщик:**
   ```
   AI-Dubbing-Studio-1.0.0-x64.exe
   ```

3. **Следуйте инструкциям установщика:**
   - Выберите папку установки (по умолчанию: `C:\Program Files\AI Dubbing Studio`)
   - Создайте ярлыки на рабочем столе и в меню Пуск (опционально)
   - Нажмите "Установить"

4. **Запустите приложение:**
   - Из меню Пуск: "AI Dubbing Studio"
   - Или с рабочего стола (если создали ярлык)
   - Или из папки установки: `C:\Program Files\AI Dubbing Studio\AI Dubbing Studio.exe`

**Преимущества:**
- ✅ Интеграция с системой
- ✅ Ярлыки в меню Пуск
- ✅ Автоматическое обновление (если настроено)

---

### Вариант 3: Распакованная версия (для разработчиков)

**Для тестирования или отладки:**

1. **Скачайте артефакт:**
   - Артефакт **"AI-Dubbing-Studio-Windows-Unpacked"**
   - Распакуйте архив

2. **Найдите исполняемый файл:**
   ```
   win-unpacked/AI Dubbing Studio.exe
   ```

3. **Запустите:**
   - Дважды кликните на `AI Dubbing Studio.exe`

**Преимущества:**
- ✅ Можно видеть структуру файлов
- ✅ Удобно для отладки
- ✅ Можно модифицировать файлы

---

## 📋 Что происходит при первом запуске?

1. **Запуск Electron** (1-2 секунды)
2. **Запуск Python backend** (5-15 секунд)
   - Загрузка Python интерпретатора
   - Инициализация Flask сервера
   - Проверка зависимостей
3. **Открытие окна приложения** (1-2 секунды)

**Итого:** 7-19 секунд при первом запуске

**Последующие запуски:** 3-5 секунд (более быстро)

---

## ⚠️ Возможные проблемы

### Проблема: "API сервер не отвечает"

**Решение:**
1. Подождите 10-30 секунд после запуска
2. Проверьте, не блокирует ли антивирус
3. Добавьте исключение в антивирус для:
   - `AI-Dubbing-Studio.exe`
   - `api-server.exe` (в папке `resources/python-backend/api-server/`)

### Проблема: Антивирус блокирует запуск

**Решение:**
1. Добавьте папку с приложением в исключения антивируса
2. Или временно отключите антивирус для первого запуска
3. Приложение безопасно - это ложное срабатывание

### Проблема: "Windows защитил ваш компьютер"

**Решение:**
1. Нажмите "Дополнительные сведения"
2. Нажмите "Выполнить в любом случае"
3. Это нормально для не подписанных приложений

### Проблема: Приложение не запускается

**Решение:**
1. Проверьте, что у вас Windows 10/11 (64-bit)
2. Убедитесь, что файл не поврежден (проверьте размер)
3. Попробуйте запустить от имени администратора
4. Проверьте логи в консоли (если доступна)

---

## 🔍 Где находятся файлы?

### Portable версия:
```
AI-Dubbing-Studio-1.0.0-x64.exe  ← Запустите этот файл
```

### Установленная версия:
```
C:\Program Files\AI Dubbing Studio\
├── AI Dubbing Studio.exe        ← Главный файл
├── resources/
│   ├── python-backend/
│   │   └── api-server/
│   │       └── api-server.exe   ← Python backend
│   ├── ffmpeg/
│   │   └── ffmpeg.exe           ← FFmpeg
│   └── src/                      ← Исходный код Python
└── locales/                      ← Языковые файлы
```

### Данные приложения:
```
%APPDATA%\AI Dubbing Studio\     ← Настройки и кэш
```

---

## 💡 Советы

1. **Создайте ярлык на рабочем столе:**
   - Правой кнопкой на `.exe` файл
   - "Создать ярлык"
   - Перетащите на рабочий стол

2. **Добавьте в автозагрузку (опционально):**
   - Win+R → `shell:startup`
   - Скопируйте ярлык в эту папку

3. **Проверьте системные требования:**
   - Windows 10/11 (64-bit)
   - 4 GB RAM минимум
   - 2 GB свободного места на диске

---

## 🆘 Поддержка

Если приложение не запускается:

1. **Проверьте логи:**
   - Откройте DevTools: `Ctrl+Shift+I` (если доступно)
   - Или запустите из командной строки для просмотра логов

2. **Проверьте антивирус:**
   - Временно отключите для теста
   - Добавьте исключения

3. **Переустановите:**
   - Удалите старую версию
   - Скачайте свежую версию
   - Запустите заново

4. **Создайте issue в GitHub:**
   - Приложите скриншоты ошибок
   - Укажите версию Windows
   - Опишите шаги для воспроизведения

---

## 📝 Резюме

**Для обычного пользователя:**
→ Используйте **Portable версию** (один EXE файл)

**Для установки в систему:**
→ Используйте **Installer** (NSIS)

**Для разработки/отладки:**
→ Используйте **Unpacked версию**
