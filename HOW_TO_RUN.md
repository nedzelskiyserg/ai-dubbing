# Как запустить AI Dubbing Studio

## 🚀 Быстрый старт

### Для разработки (macOS/Linux)

**Рекомендуемый способ - Electron приложение:**
```bash
./start_app.sh
```

Этот скрипт автоматически:
- ✅ Проверяет и устанавливает зависимости
- ✅ Запускает Python API сервер (порт 5001)
- ✅ Запускает React dev server (порт 3000)
- ✅ Открывает Electron приложение в нативном окне

### Альтернативный способ - в браузере:
```bash
./start_frontend.sh
```

Откроет приложение в браузере (Chrome/Edge/Arc) в полноэкранном режиме.

---

## 📦 Для Windows (упакованное приложение)

После сборки через GitHub Actions:

1. **Скачайте артефакт** из GitHub Actions:
   - Перейдите в раздел "Actions" вашего репозитория
   - Найдите последний успешный билд
   - Скачайте `AI-Dubbing-Studio-Windows-Installer.exe`

2. **Установите приложение:**
   - Запустите `.exe` файл
   - Следуйте инструкциям установщика
   - Приложение установится в стандартную папку программ

3. **Запустите приложение:**
   - Найдите "AI Dubbing Studio" в меню Пуск
   - Или запустите ярлык на рабочем столе

**Важно:** При первом запуске приложение может занять 10-30 секунд для запуска Python backend. Подождите, пока окно приложения откроется.

---

## 🔧 Ручной запуск (для разработки)

Если скрипты не работают, можно запустить вручную:

### Шаг 1: Установите зависимости

**Python:**
```bash
# Создайте виртуальное окружение (если еще не создано)
python3 -m venv .venv

# Активируйте его
source .venv/bin/activate  # macOS/Linux
# или
.venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt
```

**Node.js:**
```bash
cd frontend
npm install
```

### Шаг 2: Запустите API сервер

**Терминал 1:**
```bash
# Активируйте venv (если используете)
source .venv/bin/activate  # macOS/Linux
# или
.venv\Scripts\activate  # Windows

# Запустите API сервер
python3 src/api_server.py
```

Вы должны увидеть:
```
 * Running on http://127.0.0.1:5001
```

### Шаг 3: Запустите React приложение

**Терминал 2:**
```bash
cd frontend
npm start
```

React откроется на http://localhost:3000

### Шаг 4: Запустите Electron (опционально)

**Терминал 3:**
```bash
cd frontend
npm run electron-dev
```

---

## 🎯 Варианты запуска Electron

### Режим разработки (с hot reload):
```bash
cd frontend
npm run electron-dev
```

### Production режим (собранное приложение):
```bash
cd frontend
npm run electron-build
```

### Сборка для распространения:
```bash
cd frontend
npm run dist:win  # Windows
npm run dist      # Текущая платформа
```

---

## ⚙️ Настройка окружения

### Переменные окружения

Создайте файл `.env` в корне проекта (опционально):
```env
HF_TOKEN=your_huggingface_token_here
PYTHON_PATH=/path/to/python  # Если Python не в PATH
```

### Порты

- **API сервер:** http://localhost:5001
- **React dev server:** http://localhost:3000
- **Electron:** использует React dev server или собранное приложение

---

## 🐛 Решение проблем

### Ошибка "API сервер не отвечает"

1. **Проверьте, запущен ли API сервер:**
   ```bash
   curl http://localhost:5001/api/health
   ```
   Должен вернуть: `{"status": "ok"}`

2. **Проверьте, не занят ли порт 5001:**
   ```bash
   # macOS/Linux
   lsof -i :5001
   
   # Windows
   netstat -ano | findstr :5001
   ```

3. **Убейте процесс, если порт занят:**
   ```bash
   # macOS/Linux
   lsof -ti:5001 | xargs kill -9
   
   # Windows
   taskkill /F /PID <PID>
   ```

### Ошибка "Module not found"

Установите зависимости:
```bash
# Python
pip install -r requirements.txt

# Node.js
cd frontend
npm install
```

### Electron не запускается

1. Проверьте, что React приложение запущено (для dev режима)
2. Проверьте, что собрано приложение (для production режима):
   ```bash
   cd frontend
   npm run build
   ```

### На Windows: "Python не найден"

1. Убедитесь, что Python установлен и добавлен в PATH
2. Или укажите путь явно через переменную окружения:
   ```bash
   set PYTHON_PATH=C:\Python310\python.exe
   ```

---

## 📝 Структура проекта

```
ai-dubbing/
├── frontend/              # React + Electron приложение
│   ├── electron/          # Electron main и preload скрипты
│   ├── src/              # React компоненты
│   └── package.json      # Node.js зависимости
├── src/                  # Python backend
│   ├── api_server.py     # Flask API сервер
│   └── core/             # Основные модули
├── start_app.sh          # Скрипт запуска Electron (macOS/Linux)
├── start_frontend.sh     # Скрипт запуска в браузере
└── requirements.txt      # Python зависимости
```

---

## 🎬 Что дальше?

После запуска приложения:

1. **Выберите источник видео:**
   - Вставьте YouTube URL
   - Или загрузите файл через drag & drop

2. **Настройте параметры:**
   - Язык распознавания
   - Модель Whisper
   - Переводчик
   - Клонирование голоса

3. **Нажмите "START PROCESSING"**

4. **Следите за прогрессом** в терминале внизу

5. **Готовый файл** будет сохранен в папку `output/`

---

## 💡 Полезные команды

```bash
# Очистить все процессы
pkill -f "api_server" && pkill -f "react-scripts" && pkill -f "electron"

# Проверить статус API
curl http://localhost:5001/api/health

# Посмотреть логи API
tail -f /tmp/api_server.log  # если логи в файл
```

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи в терминале
2. Убедитесь, что все зависимости установлены
3. Проверьте, что порты 3000 и 5001 свободны
4. Перезапустите приложение
